// Condensez le contenu de la fonction suivante en une seule ligne,
// sans modifier le nom des fonctions/variables.
// Indice : la clé ici est de définir les valeurs par défaut des paramètres
// de la fonction.
export default function getSumOfHoods(initialNumber, expansion1989 = 89, expansion2019 = 19) {
  return initialNumber + expansion1989 + expansion2019;
}
// les valeurs par default sont indiquées directement dans la définition des paramètres.
